import type { Config } from 'prettier';

const config: Config = {
    semi: true,
    singleQuote: true,
    printWidth: 120,
    trailingComma: 'all',
    quoteProps: "consistent",
};

export default config;
